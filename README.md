# vue-hamburguer-totem-system


###After cloning this repository run "npm install" in the project folder. You can start the application with "npm run serve" and the json server with "npm run backend".
